/** Allowed DNS presets for Modularitea helpers. */
export type DnsProvider = 'cloudflare' | 'google' | 'quad9';

/** CPU governors exposed by `CpuBooster` / cpufreq. */
export type CpuProfile = 'powersave' | 'performance' | 'ondemand';
